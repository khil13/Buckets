export function ResponsibleGamblingFooter() {
  return (
    <footer className="border-t border-bucket-border py-4 text-center text-xs text-bucket-muted">
      For informational use only, not betting advice. Gambling problem? Call or text 1-800-GAMBLER.
    </footer>
  );
}
